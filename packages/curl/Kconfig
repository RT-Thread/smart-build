# Generated by smart-build package metadata. Do not edit by hand.

menu "curl command-line URL transfer tool"

config PACKAGE_CURL
    bool "curl command-line URL transfer tool"
    select PACKAGE_OPENSSL
    select PACKAGE_ZLIB
    help
      curl command-line URL transfer tool

config PACKAGE_CURL_VERSION
    string
    depends on PACKAGE_CURL
    default "8.20.0"

endmenu
