# Generated by smart-build package metadata. Do not edit by hand.

menu "zlib"

config PACKAGE_ZLIB
    bool "zlib"

config PACKAGE_ZLIB_VERSION
    string
    depends on PACKAGE_ZLIB
    default "1.3.2"

config PACKAGE_ZLIB_OPTION_SHARED
    bool "Build shared library"
    depends on PACKAGE_ZLIB
    default y

choice
    prompt "Default compression level"
    depends on PACKAGE_ZLIB
    default PACKAGE_ZLIB_OPTION_COMPRESSION_LEVEL_BALANCED

config PACKAGE_ZLIB_OPTION_COMPRESSION_LEVEL_FAST
    bool "fast"

config PACKAGE_ZLIB_OPTION_COMPRESSION_LEVEL_BALANCED
    bool "balanced"

config PACKAGE_ZLIB_OPTION_COMPRESSION_LEVEL_BEST
    bool "best"

endchoice

config PACKAGE_ZLIB_OPTION_COMPRESSION_LEVEL
    string
    depends on PACKAGE_ZLIB
    default "fast" if PACKAGE_ZLIB_OPTION_COMPRESSION_LEVEL_FAST
    default "balanced" if PACKAGE_ZLIB_OPTION_COMPRESSION_LEVEL_BALANCED
    default "best" if PACKAGE_ZLIB_OPTION_COMPRESSION_LEVEL_BEST
    default "balanced"

endmenu
